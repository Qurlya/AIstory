# Инструкция по работе с веткой парсинга


1. Убедитесь, что в `database/models/` описаны необходимые модели данных.
2. Добавьте в файл `.env` настройки подключения к базе данных:
   ```
   DB_HOST_PARSE=localhost
   ```
3. Поместите все файлы `.xlsx` для обработки в папку `data/`.

4. Запустите базу данных через Docker:

5. Запустите скрипт парсинга:
